const mongoose = require('mongoose');\n\nconst versionSchema = new mongoose.Schema({\n  version: { type: String, required: true },\n  commitHash: String,\n  commitMessage: String,\n  author: String,\n  releaseNotes: String,\n  buildNumber: Number,\n  status: { type: String, enum: ['draft', 'published', 'deployed', 'failed'], default: 'draft' },\n  deployedAt: Date,\n  deployedTo: [String],\n  installedUsers: { type: Number, default: 0 },\n  changelog: mongoose.Schema.Types.Mixed,\n  createdAt: { type: Date, default: Date.now },\n  publishedAt: Date\n}, { timestamps: true });\n\nmodule.exports = mongoose.model('VersionLog', versionSchema);